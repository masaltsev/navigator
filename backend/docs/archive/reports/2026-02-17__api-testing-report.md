# Отчёт: Проверка API по чек-листу на полупустой базе

**Дата:** 2026-02-17  
**Чек-лист:** [API_TESTING_CHECKLIST.md](../API_TESTING_CHECKLIST.md)

## Подготовка тестовых данных

Создана тестовая организация с полным набором связей:
- **ID:** `f6a8a2fa-8ae0-4b31-b99b-65fab5b0cfd0`
- **Thematic categories:** 2 (code=7 "Снижение памяти и деменция", code=12 "Зрение и здоровье глаз")
- **Organization types:** 2 (code=82 "Досуговый центр", code=92 "Клуб/Центр здоровья")
- **Specialist profiles:** 2 (code=143 "Гериатр", code=142 "Невролог")
- **Services:** 2 (code=97 "Психологическое консультирование", code=98 "Юридическая помощь")
- **Status:** `approved`

---

## Результаты проверки

### ✅ 2.1. GET /api/v1/organizations (список организаций)

#### ✅ Базовый запрос
- **Статус:** 200 OK
- **Структура ответа:** Корректная (`data[]`, `links`, `meta` с пагинацией)
- **Поля в списке:**
  - ✅ `id`, `type`, `title`, `description`
  - ✅ `organization_types` (массив объектов с `id`, `name`) — **новое поле**
  - ✅ `thematic_categories` (массив объектов) — **заменяет старые `categories`**
  - ✅ `specialist_profiles` (массив объектов) — **новое поле**
  - ✅ `services` (массив объектов)
  - ✅ `venues` (массив, пустой в тесте)

**Пример ответа:**
```json
{
  "data": [{
    "id": "f6a8a2fa-8ae0-4b31-b99b-65fab5b0cfd0",
    "organization_types": [
      {"id": 6, "name": "Досуговый центр"},
      {"id": 7, "name": "Клуб/Центр здоровья"}
    ],
    "thematic_categories": [
      {"id": 4, "name": "Снижение памяти и деменция"},
      {"id": 9, "name": "Зрение и здоровье глаз"}
    ],
    "specialist_profiles": [
      {"id": 15, "name": "Невролог"},
      {"id": 16, "name": "Гериатр"}
    ],
    "services": [...]
  }]
}
```

#### ✅ Фильтр по жизненным ситуациям (thematic_category_id[])
- **Запрос:** `GET /api/v1/organizations?thematic_category_id[]=4`
- **Результат:** Организация найдена (ID=4 соответствует code=7)
- **Статус:** Фильтр работает корректно

#### ✅ Фильтр по типам организаций (organization_type_id[])
- **Запрос:** `GET /api/v1/organizations?organization_type_id[]=6`
- **Результат:** Организация найдена (ID=6 соответствует code=82)
- **Статус:** Фильтр работает корректно, поддерживает M:N

#### ✅ Комбинированные фильтры
- **Запрос:** `GET /api/v1/organizations?thematic_category_id[]=4&organization_type_id[]=6`
- **Результат:** Организация найдена при совпадении обоих условий
- **Статус:** Комбинация фильтров работает

#### ✅ Фильтр works_with_elderly
- **Запрос:** `GET /api/v1/organizations?works_with_elderly=false`
- **Результат:** Пустой список (по умолчанию возвращаются только `works_with_elderly=true`)
- **Статус:** Фильтр работает, дефолтное поведение корректно

#### ✅ Пагинация
- **Структура:** `links` (first, last, prev, next), `meta` (current_page, per_page, total)
- **Статус:** Пагинация работает корректно

---

### ✅ 2.2. GET /api/v1/organizations/{id} (детальная карточка)

- **Запрос:** `GET /api/v1/organizations/f6a8a2fa-8ae0-4b31-b99b-65fab5b0cfd0`
- **Статус:** 200 OK
- **Структура ответа:**
  - ✅ Все поля организации (`inn`, `ogrn`, `site_urls` — null в тесте)
  - ✅ Массив `venues` (пустой в тесте)
  - ✅ Массивы `thematic_categories`, `organization_types`, `specialist_profiles`, `services` — **все новые поля присутствуют**
  - ✅ Массивы `events`, `articles` (пустые в тесте)
- **Статус:** Детальная карточка возвращает полную структуру с новыми полями

---

### ⚠️ Не проверено (требует дополнительных данных)

#### 2.1. Дополнительные фильтры
- Фильтр по городу (`city_fias_id`) — требует venues с fias_id
- Фильтр по услугам (`service_id[]`) — можно проверить, но нужны дополнительные организации
- Георадиусный фильтр (`lat`, `lng`, `radius_km`) — требует venues с координатами

#### 2.3. GET /api/v1/events
- Требует создания событий и event_instances

#### 3. Внутренний API `/api/internal`
- Требует аутентификации и тестовых запросов импорта

---

## Выводы

### ✅ Что работает корректно

1. **Структура ответов обновлена:**
   - `organization_types` (массив) вместо `organization_type` (один объект)
   - `thematic_categories` вместо `categories` / `problem_categories`
   - `specialist_profiles` — новое поле присутствует

2. **Фильтры работают:**
   - `thematic_category_id[]` — фильтрация по жизненным ситуациям
   - `organization_type_id[]` — фильтрация по типам организаций (M:N)
   - Комбинация фильтров работает корректно
   - `works_with_elderly` — дефолтное поведение корректно

3. **Пагинация:** Структура `links` и `meta` корректна

### 📝 Рекомендации для полной проверки

1. **Создать тестовые данные:**
   - Организации с venues (fias_id, coordinates)
   - Организации с разными статусами (draft, pending_review, rejected)
   - События (events, event_instances)

2. **Проверить:**
   - Георадиусный фильтр с реальными координатами
   - Фильтр по городу через fias_id
   - Внутренний API импорта с полным JSON

3. **Проверить edge cases:**
   - Организации без связей (пустые массивы)
   - Организации со статусом != approved (не должны возвращаться)
   - Несуществующие UUID (404)

---

## Статус проверки

| Раздел | Статус | Примечание |
|--------|--------|------------|
| Базовый запрос списка | ✅ | Структура корректна, новые поля присутствуют |
| Детальная карточка | ✅ | Все новые поля возвращаются |
| Фильтр thematic_category_id | ✅ | Работает |
| Фильтр organization_type_id | ✅ | Работает (M:N) |
| Комбинированные фильтры | ✅ | Работают |
| Фильтр works_with_elderly | ✅ | Дефолт корректный |
| Пагинация | ✅ | Структура корректна |
| Фильтр по городу | ⏸️ | Требует venues с fias_id |
| Георадиусный фильтр | ⏸️ | Требует venues с coordinates |
| События API | ⏸️ | Требует создания событий |
| Внутренний API | ⏸️ | Требует аутентификации |

**Общий статус:** ✅ Основные эндпоинты работают корректно с новой структурой данных. API готов к использованию после наполнения тестовыми данными для полной проверки всех фильтров.
